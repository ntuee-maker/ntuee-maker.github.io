// @flow

import React from 'react';

import styles from './acativities.module.scss';

const Acativitys = () => (
  <div className={styles.wrapper}>
    acativity
  </div>
);

export default Acativitys;
